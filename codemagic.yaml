workflows:
  android-release:
    name: Chain Gang Poker — Android Release AAB
    environment:
      java: 17
      node: 20
      android_signing:
        - chaingang_keystore
      vars:
        VITE_API_BASE_URL: https://chainggangpoker.com
        VITE_SHARE_ORIGIN: https://chainggangpoker.com
    scripts:
      - name: Install npm dependencies
        script: |
          rm -f package-lock.json
          unset NPM_CONFIG_REGISTRY
          npm install --registry https://registry.npmjs.org/

      - name: Build web app
        script: npm run build

      - name: Capacitor sync Android
        script: npx cap sync android

      - name: Make Gradle wrapper executable
        script: chmod +x android/gradlew

      - name: Bundle release AAB
        script: cd android && ./gradlew bundleRelease --no-daemon
    artifacts:
      - android/app/build/outputs/bundle/release/app-release.aab

  ios-release:
    name: Chain Gang Poker — iOS Release IPA
    max_build_duration: 60
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.dgmentertainment.poker
      xcode: latest
      cocoapods: default
      node: 20
      vars:
        VITE_API_BASE_URL: https://chainggangpoker.com
        VITE_SHARE_ORIGIN: https://chainggangpoker.com
    scripts:
      - name: Install npm dependencies
        script: |
          rm -f package-lock.json
          unset NPM_CONFIG_REGISTRY
          npm install --registry https://registry.npmjs.org/

      - name: Build web app
        script: npm run build

      - name: Capacitor sync iOS
        script: npx cap sync ios

      - name: Install CocoaPods
        script: cd ios/App && pod install

      - name: Build signed IPA for App Store
        script: |
          xcodebuild \
            -workspace ios/App/App.xcworkspace \
            -scheme App \
            -configuration Release \
            -sdk iphoneos \
            -archivePath ios/App/App.xcarchive \
            -allowProvisioningUpdates \
            archive
          xcodebuild \
            -exportArchive \
            -archivePath ios/App/App.xcarchive \
            -exportPath ios/App/output \
            -exportOptionsPlist ios/App/ExportOptions.plist
    artifacts:
      - ios/App/output/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_PRIVATE_KEY
        key_id: $APP_STORE_CONNECT_KEY_IDENTIFIER
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
        submit_to_testflight: true
